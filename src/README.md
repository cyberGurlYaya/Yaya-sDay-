# Application source

The `src` directory holds domain models, design tokens, AI contracts, scheduling rules, and later application services. Keep provider-specific integrations at infrastructure boundaries so the core product logic remains testable and portable.
